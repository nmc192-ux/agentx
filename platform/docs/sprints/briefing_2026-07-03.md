# Briefing — 3 July 2026

**Run:** Autonomous Sprint Loop, first unattended hand-off. Phase A, Sprint 9 — Stabilize.
**Branch:** `phase-a-autonomous` (pushed). **Not merged. Not on main. Not live on agentx.social.**

## Bottom line
I stopped early — deliberately — because Sprint 9's detailed spec file doesn't exist and half the sprint hits your non-negotiable safety boundary (production infra, PyPI publish, prod DB) or sits in tangled version control. I did the one high-value, fully-safe, read-only deliverable the rest of the sprint depends on: a **Fable-5 router safety audit** that classifies all 20 gated routers and found **three concrete defects, one of them a wallet-drain security hole.** Your move: read the audit's recommendation, confirm the reconstructed scope, and tell me which of the remaining items you want the loop to execute vs. keep for yourself.

## Stopping reason
**Scope surprise** (protocol stopping condition). Three compounding surprises, none answerable from the founding docs:
1. `platform/docs/sprints/sprint_9_stabilize.md` — the file the hand-off says to execute — **does not exist** anywhere (disk, all branches, git history). `state_of_agentx.md` itself says it was never committed. I reconstructed Sprint 9's scope from `strategic_plan_v2.md §Sprint 9`, but that's a one-paragraph summary, not the acceptance criteria a high-stakes stabilization sprint needs.
2. **"Re-enable routers" is a production infra action, not a repo change.** Gating lives in the `DISABLED_ROUTERS` env var on the Fly.io prod app; the code default is empty and it's not in `fly.toml`. The loop can't (and per the safety boundary shouldn't) flip that — it can only audit and recommend. Which it did.
3. **The SDK is a tangled dual-repo.** `sdk/` is a nested independent git repo (own remote `agentx-sdk.git`, on branch `feat/memory-namespace` with uncommitted changes) whose files are *also* tracked by the parent repo. Fixing "the 5 failing SDK tests" and committing cleanly here isn't safe.

On a first unattended run, bulldozing reconstructed scope across those hazards is exactly what the protocol says not to do. So: audit + brief + stop, branch clean.

## What shipped (on branch `phase-a-autonomous`, not yet merged)
- **Router safety audit** → this briefing + the table below. One commit: the branch, the `platform/docs/sprints/` directory, and this file.
- No code was changed. No routers were enabled/disabled. No packages published. No DB touched. The only working-tree noise is `graphify-out/GRAPH_REPORT.md`, which a `graphify watch` hook auto-regenerates — I kept it out of every commit.

## The audit result (the deliverable)

**Method:** static code audit on **Fable 5** — read every gated router + its service layer, cross-checked backing tables against `scripts/init-db.sql` + alembic revisions 002–038, scanned for stubs (zero stub markers found — all handlers are real SQL). Server was **not** run; verdicts assume prod is at alembic head. Confidence caveats are in the audit.

**Recommended production value:**
```
DISABLED_ROUTERS=governance,consensus,graph,agent_economy,nodes
```
**Enable the other 15** (memory, collectives, tasks, wallets, stakes, economy, contracts, agentbus, verifications, markets, communities, conversations, channels, rooms, pulse) — in cohorts: token stack together, social stack together.

**Keep the 5 disabled, and why:**
| Router | Reason | Severity |
|---|---|---|
| `agent_economy` | `POST /markets/bounties/auto` takes `agent_did` from the request body with **no JWT** → any anonymous caller can escrow-drain any agent's wallet (`agent_economy.py:51` → `auto_bounty_service.py:79`) | **Security — high** |
| `nodes` | register/event-injection endpoints **intentionally unauthenticated**, hardening deferred in-code → spam/spoof of federated peers (`node_router.py:14–19`) | **Security — med** |
| `governance` | writes to `governance_votes`, a table **no migration creates** (silent name collision: baseline already owns `votes` with a different schema, so migration 020's `CREATE IF NOT EXISTS` is a no-op) → every vote cast 500s (`governance_service.py:281,307`) | Correctness |
| `consensus` | depends on governance proposals + a `votes` table nothing writes to → permanently empty tallies (`consensus_service.py:198`) | Correctness |
| `graph` | default `GET /graph/constellation` JOINs nonexistent `room_members` (table is `room_participants`) → 500 (`graph_service.py:159`; `include_rooms` defaults True) | Correctness |

Three are cheap fixes (one migration for `governance_votes`; one-line `room_members`→`room_participants` rename; one `Depends(get_current_agent)` on the auto-bounty route). I did **not** apply them — see "Decisions I need from you."

## Model usage
- **Fable 5** (free-window, top tier): the router safety audit — the exact Tier-1 "a wrong enable exposes broken/unsafe endpoints to real users" task. ~84k tokens, 45 tool calls, one delegated subagent.
- **Opus 4.8**: kickoff reconnaissance, scope reconstruction, this briefing (judgment work).
- **Note on `/model`:** I can't switch models interactively in this Claude Code SDK context, but the Agent tool exposes a `fable` model, so Fable 5 *is* reachable via delegated subagents — which is how the audit ran. No Tier-1 step was downgraded to Opus.

## Test results
Honest coverage: **no code changed, so nothing to test.** The audit is static analysis, not a live probe — it did not run the server or query a DB. Its own "confidence & method limits" section flags the verdicts it's least sure about (`economy` treasury on an unseeded table, `tasks` whose tables were reportedly missing on staging, CHECK-constraint enum values in `pulse`/`rooms`). Local stack *can* run (Docker daemon up, compose valid) — I just didn't need it for a read-only audit.

## What merging this branch will do
This branch only adds docs, so merging it is low-risk. But for the broader Sprint 9 work, here's the pipeline you asked me to record:
- **Backend:** merge → CI → on success, auto-deploy to **staging** (Fly.io) → **production is gated behind a GitHub Environment manual-approval** step. Safe.
- **Frontend/UI:** **not** in this workflow — Vercel's Git integration **auto-deploys `main` to production `agentx.social` with no manual gate** (only "wait for CI"). ⚠️ **Any UI change merged to main goes live immediately.** Relevant because the `.well-known/*` fix (Sprint 9 item) is a UI/edge change.
- **`DISABLED_ROUTERS` is not affected by a merge at all** — it's a Fly.io runtime env var you set separately. Enabling routers = you editing that value, independent of git.

## Decisions I made (reversible)
- Stashed the stray `graphify-out/GRAPH_REPORT.md` on `main` before branching (regenerated by a hook; `git stash list` to recover). Rationale: keep the branch clean; it's a generated artifact, not your work.
- Reconstructed Sprint 9 scope from `strategic_plan_v2.md` since the spec file is missing. Rationale: it's the authoritative source; documented so you can correct it.
- Ran the audit as a **static** code audit rather than bringing up the full stack and probing 20 endpoints unattended. Rationale: tractable, reproducible, and the right granularity for a READY/PARTIAL/MOCK first pass; dynamic smoke-testing is a natural follow-up.

## Decisions I need from you
1. **The missing spec.** Do you have `sprint_9_stabilize.md` to drop in, or should I treat the `strategic_plan_v2.md` summary as the spec? This determines the acceptance criteria for everything downstream.
2. **The `DISABLED_ROUTERS` value.** Do you accept `governance,consensus,graph,agent_economy,nodes` as the disabled set (enable the other 15)? You apply this on Fly.io; the loop can't.
3. **The three cheap fixes.** Want the loop to fix them on-branch (with local verification once the stack + a scratch DB are up)? One of them — adding auth to the wallet-drain endpoint — is **security-sensitive code**, which the protocol says to escalate rather than do unattended. My recommendation: **yes to the graph typo and governance migration; the auth fix I'd rather you review the diff on before I commit it.**
4. **Which remaining Sprint 9 items go to the loop vs. you?** My read: loop-safe = Celery-beat/Trust-recalc wiring (after audit-confirmed inputs), the seed-agent dedupe (local DB only). Yours = flipping the prod `DISABLED_ROUTERS`, the PyPI `agentx-client`→`agentx-py` publish (irreversible), the `.well-known/*` edge fix (only reproduces in the prod edge split — not locally verifiable). And the SDK dual-repo tangle needs untangling before its tests can be touched.

## Recommended next action
Spend five minutes on the audit table above. Then: (a) confirm or supply the spec, (b) accept/adjust the `DISABLED_ROUTERS` set and apply it on Fly.io, (c) tell me which of the four decisions to run. The moment you confirm scope, the loop can execute the genuinely-safe items (Celery-beat wiring, seed dedupe, the two non-auth fixes) with local verification and report back.

## Live-test checklist for after you (eventually) enable routers
Since the loop can't test prod, when you set `DISABLED_ROUTERS` on Fly.io and deploy:
- Run the alembic suite against a scratch DB **first** and smoke-test one endpoint per newly-enabled router — the audit assumes prod is at head; confirm it.
- Verify `governance`, `consensus`, `graph`, `agent_economy`, `nodes` genuinely return 404 (disabled), not 500.
- Hit `GET /graph/constellation` (default params) — confirm it's disabled, because enabled it 500s today.
- Confirm `/.well-known/agent.json` and `/.well-known/skill.md` resolve on `agentx.social` (the edge-routing bug) — this is UI-side and unrelated to `DISABLED_ROUTERS`.
- Spot-check that no unauthenticated call to `agent_economy`'s auto-bounty route can move wallet funds (should be 404 while disabled).

---
*Branch `phase-a-autonomous` is clean, pushed, and buildable. Nothing is live. Awaiting your call.*
