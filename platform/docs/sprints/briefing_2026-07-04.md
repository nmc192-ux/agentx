# Briefing — 4 July 2026

**Run:** Autonomous Sprint Loop, second hand-off. Phase A, Sprint 9a — Router gating into the repo.
**Branch:** `phase-a-autonomous`, commit `055c5f2` (pushed). **Not merged. Not on main. Not live on agentx.social.**

## Bottom line
Sprint 9a is **built and locally verified** — router gating now lives in commented repo config with the env var as an emergency override, startup logging, and zero tests broken (2031 passed). But I found one thing you must confirm before merge: **the hand-off assumed production disables 5 routers; the documented evidence says it disables ~19.** The "5" is the Sprint 9 *target*, not the current state. To keep the sprint's zero-behavior-change promise, I set the repo default to the **19-router set** the 5 May audit records as live. **Your one action: confirm the actual Fly.io `DISABLED_ROUTERS` value matches those 19, then review and merge.**

## Stopping reason
**Sprint complete, with one flagged decision** (Step 5's "explicit, flagged, reasoned difference for DrJ to approve"). Every acceptance criterion is met except the parity confirmation, which I cannot finish myself because reading the live Fly.io value is outside the safety boundary. The hand-off's own CRITICAL instruction — *"if the current production value differs from those five, STOP and flag it"* — triggered, so I'm flagging rather than silently diverging.

## The parity finding (the thing to confirm)
- The hand-off states production currently disables exactly `governance, consensus, graph, agent_economy, nodes` (5).
- `platform/docs/audit/audit_2026-05-05.md:304` (corroborated by `strategic_plan_v2.md:39`) documents production as disabling **~19 routers — every gated router except `memory`** (≈130 of 184 endpoints returning 404).
- So the **5 is the Sprint 9 target** (what should be off *after* the fixes and enablement), **not today's state.** Had I set the repo default to 5, merging + removing the env var would have **enabled ~14 currently-off routers at once** — the exact behavior change 9a exists to avoid.
- **Resolution:** repo default = the documented 19-router set → parity preserved → zero behavior change. I inferred "19" from the audit, not from Fly.io. **Please confirm the live value matches before removing the env var.**

Why this is safe even if my inferred 19 is slightly off: **while the Fly.io `DISABLED_ROUTERS` env var stays set, it overrides the repo default entirely** (verified locally). So merging 9a is zero-change regardless of the repo list. The repo default only takes effect if/when you *remove* the env var — a deliberate, reviewed step.

## What shipped (on branch `phase-a-autonomous`, not merged) — commit `055c5f2`
- **`platform/src/router_config.py`** (new): `DEFAULT_DISABLED_ROUTERS`, each entry commented with why it's off + re-enable condition. Two tiers: `BROKEN_OR_INSECURE` (5) and `PARITY_HOLD` (14 — audit-cleared, off in prod today, enabled deliberately in Sprint 9).
- **`platform/src/config.py`**: `disabled_routers` defaults to the repo list; `DISABLED_ROUTERS` env var overrides it (Pydantic precedence = emergency kill-switch); new `disabled_routers_source` for logging.
- **`platform/src/main.py`**: startup logs the effective disabled list + its source.
- **`platform/conftest.py`**: tests set `DISABLED_ROUTERS=""` so the full API surface registers regardless of the production default (preserves pre-9a test behavior; without it, 17 router-test files would newly 404).
- Retro: `sprint_9a_retro.md`. State updated: `state_of_agentx.md`.

## Model usage
- **Opus 4.8**: kickoff, the parity investigation and 5-vs-19 judgment (the safety-critical Step 5 call), config/wiring design, this briefing.
- **Sonnet-tier mechanical work** done inline: the edits, curl checks, test runs.
- **Fable 5:** *not* separately invoked this sprint. The designated Tier-1 step (Step 5 parity) turned out to be a documentary determination — the audit explicitly lists the current set — resolved by reading evidence, not model judgment, so escalation added no value. The judgment that remained (what to *do* about 5-vs-19) is the decision I'm escalating to you. Flagging this so you know no step was under-modeled: the parity call rests on the audit's documented list plus your confirmation, not on an autonomous guess.

## Test results (honest coverage)
- **Mechanism, real Pydantic `Settings` (host venv):** no env → repo default (19, `memory` enabled); env set → override (exactly the env value); empty env → 0 disabled. All pass.
- **Live stack (docker compose, no `DISABLED_ROUTERS` set → repo default):** startup log emits `Router gating: 19 disabled via repo default … — agent_economy,agentbus,…,wallets`. Curl: 5 sampled disabled routers → 404; `/health`, `/agents/discover` → 200; `/feed` → 422 (registered). OpenAPI: `memory` registered under `/agents/{did}/memory`; **none of the 19 disabled routers leaked**.
- **Full test suite (in-container):** **2031 passed, 14 skipped** (skips are `--db`/`--docker` integration tests, off by default). No regressions.
- **Not tested:** production behavior (out of boundary); whether the live Fly.io value equals the documented 19 (the open item above). Note: I had to rebuild the local api image (2-month-old image lacked `sentry_sdk`, which is in `requirements.txt`) and install the test deps into the container ad-hoc — the prod image ships without pytest. Both are pre-existing local-env drift, not caused by this sprint, but worth a cleanup pass someday.

## What merging this branch will do
- **Backend:** merge → CI → auto-deploy to **staging** (Fly.io) → **production gated behind manual approval**. This branch is code + docs; the code change is behavior-neutral **as long as the Fly.io `DISABLED_ROUTERS` env var remains set** (it overrides the repo default).
- **The behavior change only happens when you remove the env var** — at which point the repo's 19-router default becomes authoritative. Confirm parity first.
- No UI changes in this branch, so no Vercel production effect from 9a itself.

## Decisions I made (reversible)
- **Repo default = 19 (documented production), not 5 (hand-off's assumption).** Rationale: the sprint's overriding rule is zero-behavior-change = match current production; the audit documents 19. Fully reversible — it's one edit to `router_config.py`.
- Placed the config as a flat `router_config.py` module (no `config/` package exists; `config.py` is a single file) to match project conventions.
- Split the list into `BROKEN_OR_INSECURE` vs `PARITY_HOLD` tiers so the "must stay off" vs "off only for parity" distinction isn't lost — makes Sprint 9's enablement a clean, well-labeled edit.
- Added `DISABLED_ROUTERS=""` to `conftest.py` so tests keep exercising the full surface. Reversible; documented in-file.

## Decisions I need from you
1. **Confirm the live Fly.io `DISABLED_ROUTERS` value.** Does it match the 19-router set (all gated except `memory`)? If it disables *more* or *fewer*, tell me and I'll reconcile `router_config.py` to true parity. **This is the one blocker to a clean merge.**
2. **After merge, env var: keep or remove?** Recommended once you've confirmed parity: remove it so the repo is the sole source of truth. Until then it's harmless belt-and-suspenders (it overrides the repo default identically).

## Recommended next action
Confirm the live `DISABLED_ROUTERS` value against the documented 19 (2 minutes on the Fly.io dashboard). If it matches, review the diff and merge — behavior stays identical. Then Sprint 9 proper (fix-then-enable) can run: fix the 3 defects and enable the 14 `PARITY_HOLD` routers in cohorts by editing `router_config.py` on a branch.

## Live-test checklist for after you merge
- Confirm the effective gating on `agentx.social` is unchanged from before the merge (same features on/off) — this sprint must be zero behavior change.
- Check the backend startup logs show the `Router gating: N disabled via …` line, and that the source (env override vs repo default) is what you expect for prod.
- If you remove the env var: re-confirm the same 19 are still disabled (now via the repo default) and nothing newly 500s or newly appears.

---
*Branch `phase-a-autonomous` is clean, pushed, and buildable. Nothing is live. Awaiting your confirmation on the parity value.*
