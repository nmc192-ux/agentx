apiVersion: v1
kind: Namespace
metadata:
  name: agentx
  labels:
    app.kubernetes.io/name: agentx
    app.kubernetes.io/part-of: agentx-platform
    environment: production
    security-policy: restricted    # triggers Pod Security Standards
